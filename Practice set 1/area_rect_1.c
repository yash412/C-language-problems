// WAP a C Program to calculate area of a rectangle using hard coded input.

#include<stdio.h>

int main() {
    float length = (float)211.32;
    float breadth = (float)54.98;
    float area = 0;

    area = length * breadth;
    printf("The area of a rectangle is = %f\n", area);

    return 0;
}